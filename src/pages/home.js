import React, { useState } from "react";
import Navbar from "../components/navbar/Navbar";

const Home = () => {
  return (
    <main>
      <header className="bg">
        <Navbar logIn={true} />
      </header>
    </main>
  );
};

export default Home;

//main page
